
function iterativeLog(array){array.forEach(callback2)}

function callback2(element, index, array){console.log(index + ": " + element)}

function iterate(callback){var array = ["horse"]; array.forEach(callback); return array;}

function doToArray(array,cb){array.forEach(cb);}
